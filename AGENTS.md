## Agent 技能

### Issue 跟踪器

本仓库的需求、规格和开发任务统一使用 GitHub Issues 管理。详见 `docs/agents/issue-tracker.md`。

### 分诊标签

使用 Matt Skills 默认的分诊标签词汇。详见 `docs/agents/triage-labels.md`。

### 领域文档

本仓库使用单上下文（single-context）的领域文档布局。详见 `docs/agents/domain.md`。
